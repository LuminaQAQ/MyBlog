# 002\_键盘控制 CLI

## 所需依赖

```js
import readline from "readline";
```

## 前置基础

1. 在 `node` 中实现键盘输入反馈, 是先通过"注册"键盘输入事件, 再对进程进行键盘事件监听.

```js
readline.emitKeypressEvents(process.stdin); // 支持 输入流 处理键盘事件

process.stdin.on("keypress", (str, key) => {
  // ....
});
```

2. 在 `CLI` 中, 通常需要手动控制终端键盘事件, 以支持更优秀的操作方式/习惯.

```js
process.stdin.setRawMode(true); // 禁用内置键盘事件, 如: 键入后, 按下回车表示确认; ctrl + c 表示退出终端.

// 实现 ctrl + c 退出终端
process.stdin.on("keypress", (str, key) => {
  if (key.sequence === "\u0003") {
    process.exit();
  }
});
```
