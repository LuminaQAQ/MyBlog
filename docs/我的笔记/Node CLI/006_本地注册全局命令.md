# 006\_本地注册全局命令

1. 在 `package.json` 添加 `bin` 字段，指定命令名和对应的文件路径：

![配置 bin 字段](./images/006/配置bin字段.png)

2. 在对应的文件开头加上:

```ts
#!/usr/bin/env node
```

用于告诉 `shell` 用 `node` 执行该文件

3. 在项目根目录执行:

```shell
npm link
```
